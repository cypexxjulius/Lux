#pragma once

namespace Lux::GUI
{

enum class LayoutSpacing
{
	MIDDLE,
	START,
	END,
	MAX_BETWEEN,
	DISTRIBUTED,
};

enum class ScaleType
{
	ZERO_SPACE,
	FIXED,
	DYNAMIC,
};

enum class LayoutOrientation
{
	HORIZONTAL,
	VERTICAL,
};

}
